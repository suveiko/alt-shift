# Main application deployment
[build]
  command = "pnpm build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "9.0.0"

# Redirects for SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
